{
  "name": "@uni/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "//react-dom": "Pinned even though nothing here renders to the DOM. expo-router declares react-dom as an optional peer, so with no pin npm installs the latest (19.2.8), which requires React ^19.2.8 and conflicts with the React 19.1.0 that SDK 54 needs. Newer npm refuses to install at all; older npm resolves it into a lockfile that only works by luck.",
  "scripts": {
    "start": "expo start",
    "ios": "expo run:ios",
    "typecheck": "tsc --noEmit",
    "android": "expo run:android"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "@supabase/supabase-js": "^2.47.0",
    "@tanstack/react-query": "^5.62.0",
    "expo": "~54.0.36",
    "expo-constants": "~18.0.13",
    "expo-linking": "~8.0.12",
    "expo-router": "~6.0.24",
    "expo-status-bar": "~3.0.9",
    "expo-web-browser": "~15.0.11",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-screens": "~4.16.0",
    "react-native-url-polyfill": "^2.0.0",
    "react-native-webview": "13.15.0"
  },
  "devDependencies": {
    "@types/react": "~19.1.10",
    "typescript": "~5.9.2"
  }
}
